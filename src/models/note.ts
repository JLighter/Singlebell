export class Note {

  constructor(
    public name: String,
    public fileName: String,
    public postion : Number
  )
  {
    this.name = name;
    this.fileName = fileName;
    this.postion = postion;
  }
}
